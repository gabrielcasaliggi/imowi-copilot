services:
  copilot:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATA_DIR: /app/data
    volumes:
      # Tickets y sesiones persisten acá
      - copilot-data:/app/data
    restart: unless-stopped

volumes:
  copilot-data:
